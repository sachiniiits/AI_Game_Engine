# AI_Game_Engine
A cross-platform game engine with a Rust core compiled to WebAssembly, using wgpu targeting WebGPU, WebGL2, Vulkan, Metal, and DX12 from a single codebase - deployed to Web, Desktop (Tauri), iOS, and Android (Capacitor) with zero platform-specific branches.
