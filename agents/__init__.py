# V2 Pipeline Agents
# research_agent  — web-searches for game mechanics and patterns
# script_agent    — writes game design doc using research
# logic_agent     — writes Phaser-aware pseudocode + draw manifest
# coding_agent    — generates single-file Phaser 3 HTML game
# validator_agent — validates game with static checks + headless browser
